package TwitterSettings

// TwitterSettings is a struct to manage the 
// tokens required for usage of the twitter API 
type TwitterSettings struct {
    ConsumerKey string
    ConsumerSecret string
    AccessToken string
    AccessTokenSecret string
}
